/**
 * take controller inputs and translate them to desired state commands,
 * send those to the pico via serial. read state data from the pico via serial,
 * and send that back to the base station. handle Loss-of-Signal (LoS).
 */