/**
 * This is the code on the pico.
 * It must take desired state commands from the pi 5 via serial,
 * translate those to motor/servo commands, read sensor input,
 * run control loops, and send the robot state back to the pi via serial.
 */